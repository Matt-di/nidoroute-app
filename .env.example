# API Configuration
API_BASE_URL=https://nidoroute.filmbet.net/api/v1
WS_URL=wss://nidoroute.filmbet.net
# Optional: if your backend uses a different path for driver location updates (must include {driver})
# DRIVER_LOCATION_ENDPOINT=/drivers/{driver}/location

# Maps
GOOGLE_MAPS_API_KEY=dummy_google_maps_api_key_replace_with_real_key

# App Settings
CONNECTION_TIMEOUT=30000
LOCATION_UPDATE_INTERVAL=10

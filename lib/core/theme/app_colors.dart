import 'package:flutter/material.dart';

class AppColors {
  // Brand
  static const Color primary = Color(0xFF03045E);
  static const Color secondary = Color(0xFF0077B6);

  // Accent
  static const Color teal = Color(0xFF2EC4B6);

  // Backgrounds
  static const Color background = Color(0xFFEDFCFF);
  static const Color surface = Colors.white;

  // Status
  static const Color error = Color(0xFFC1121F);
  static const Color warning = Color(0xFFFCA311);

  // Text
  static const Color textPrimary = Color(0xFF0B132B);
  static const Color textSecondary = Color(0xFF5C677D);

  // Dark mode
  static const Color darkBackground = Color(0xFF0B1020);
  static const Color darkSurface = Color(0xFF161A2B);
}
